machine:
  environment:
    XCODE_SCHEME: threepio
    XCODE_WORKSPACE: threepio
  xcode:
    version: 8.0

dependencies:
  pre:
    - npm install -g appdmg
  post:
    - appdmg dmg/threepio-dmg.json dmg/target/threepio.dmg
